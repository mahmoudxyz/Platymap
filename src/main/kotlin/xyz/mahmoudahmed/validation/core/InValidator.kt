package xyz.mahmoudahmed.validation.core

import xyz.mahmoudahmed.validation.rules.DataValidator

/**
 * Validator for in-mapping validation (during mapping process).
 */
class InValidator : DataValidator()