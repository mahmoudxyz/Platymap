package xyz.mahmoudahmed.format

/**
 * Enum representing supported data formats
 */
enum class Format {
    JSON,
    XML,
    CSV,
    YAML,
    PROPERTIES,
    AVRO,
    PROTOBUF,
    PARQUET,
    EXCEL,
    UNKNOWN,
    JAVA_BEAN
}